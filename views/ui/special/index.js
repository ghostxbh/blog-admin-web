const specialList = require('./special-list');
const specialInfo = require('./special-info');
const specialAdd = require('./special-add');

module.exports = {
    specialList,
    specialInfo,
    specialAdd
};