const contentAdd = require('./content-add');
const contentList = require('./content-list');
const contentInfo = require('./content-info');
module.exports = {
    contentAdd,
    contentList,
    contentInfo
};